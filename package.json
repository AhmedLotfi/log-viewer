{
  "name": "log-viewer",
  "version": "0.1.0",
  "description": "Single-page Serilog log viewer. No build step.",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  }
}
